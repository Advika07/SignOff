'use client'

import { useState, useRef } from 'react'
import { Button } from '@/components/ui/button'
import {
  ArrowRight,
  BarChart3,
  BookOpen,
  Camera,
  Check,
  ChevronRight,
  CircleUserRound,
  Flame,
  HelpCircle,
  Home,
  Lock,
  Menu,
  Play,
  RotateCcw,
  Sparkles,
  Trophy,
  UserRound,
  X,
} from 'lucide-react'
import { useSignRecognition } from '@/lib/useSignRecognition'

type View = 'home' | 'practice' | 'progress' | 'profile'
type PracticeState = 'intro' | 'permission' | 'searching' | 'almost' | 'correct' | 'incorrect'

const lessons = [
  { title: 'Hello', subtitle: 'Meet & greet', level: 'Level 1', color: 'bg-primary', done: true },
  { title: 'Thank you', subtitle: 'Show appreciation', level: 'Level 1', color: 'bg-accent', done: false },
  { title: 'My name is…', subtitle: 'Introduce yourself', level: 'Level 1', color: 'bg-secondary', done: false },
]

function Logo() {
  return <div className="flex items-center gap-2 font-serif text-xl font-bold tracking-tight"><span className="grid size-8 place-items-center rounded-xl bg-primary text-primary-foreground"><Check className="size-4" strokeWidth={3} /></span>SignOff</div>
}

function Sidebar({ view, setView }: { view: View; setView: (view: View) => void }) {
  const links = [
    { id: 'home' as View, label: 'Learn', icon: Home },
    { id: 'practice' as View, label: 'Practice', icon: Camera },
    { id: 'progress' as View, label: 'Progress', icon: BarChart3 },
    { id: 'profile' as View, label: 'Profile', icon: UserRound },
  ]
  return <aside className="hidden w-64 shrink-0 flex-col border-r border-border bg-card px-5 py-6 lg:flex"><Logo /><p className="mt-12 px-3 font-mono text-[10px] uppercase tracking-[0.2em] text-muted-foreground">Your journey</p><nav className="mt-3 flex flex-col gap-1">{links.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => setView(id)} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-medium transition-colors ${view === id ? 'bg-primary text-primary-foreground' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon className="size-4" />{label}</button>)}</nav><div className="mt-auto rounded-2xl bg-muted p-4"><div className="flex items-center justify-between"><span className="font-mono text-[10px] uppercase tracking-wider text-muted-foreground">Current streak</span><Flame className="size-4 text-accent" /></div><p className="mt-2 text-2xl font-semibold">3 days</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-background"><div className="h-full w-3/5 rounded-full bg-accent" /></div><p className="mt-2 text-xs text-muted-foreground">Keep it going!</p></div></aside>
}

function Header({ setView }: { setView: (view: View) => void }) {
  return <header className="flex items-center justify-between border-b border-border bg-background px-5 py-4 lg:px-10"><div className="lg:hidden"><Logo /></div><div className="hidden items-center gap-2 text-sm text-muted-foreground lg:flex"><span>Monday, June 12</span><span className="size-1 rounded-full bg-border" /><span>Good afternoon, Alex</span></div><div className="flex items-center gap-3"><button aria-label="Help" className="hidden rounded-full p-2 text-muted-foreground hover:bg-muted sm:block"><HelpCircle className="size-5" /></button><button onClick={() => setView('profile')} aria-label="Open profile" className="grid size-9 place-items-center rounded-full bg-accent font-serif font-bold text-accent-foreground">A</button><button aria-label="Open menu" className="rounded-lg p-2 text-muted-foreground hover:bg-muted lg:hidden"><Menu className="size-5" /></button></div></header>
}

function HomeView({ setView, startLesson }: { setView: (view: View) => void; startLesson: () => void }) {
  return <div className="mx-auto w-full max-w-6xl px-5 py-8 lg:px-10 lg:py-12"><section className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="font-mono text-xs uppercase tracking-[0.22em] text-accent-foreground/70">Your learning path</p><h1 className="mt-3 max-w-xl font-serif text-4xl leading-tight tracking-tight text-foreground md:text-6xl">Say more with your hands.</h1><p className="mt-4 max-w-md text-base leading-7 text-muted-foreground">Build real confidence in American Sign Language, one small conversation at a time.</p></div><div className="flex gap-2"><Button variant="outline" onClick={() => setView('progress')}><BarChart3 data-icon="inline-start" />View progress</Button></div></section><section className="mt-10 grid gap-5 md:grid-cols-[1.35fr_0.65fr]"><div className="relative overflow-hidden rounded-[2rem] bg-primary p-7 text-primary-foreground md:p-10"><div className="relative z-10 max-w-md"><span className="inline-flex items-center gap-2 rounded-full bg-primary-foreground/10 px-3 py-1.5 font-mono text-[10px] uppercase tracking-widest"><Sparkles className="size-3" />Up next</span><h2 className="mt-6 font-serif text-4xl leading-tight">Level 1: First signs</h2><p className="mt-3 leading-7 text-primary-foreground/70">Learn the foundations for your first hello, introduction, and thank you.</p><Button className="mt-8 bg-primary-foreground text-primary hover:bg-primary-foreground/90" onClick={startLesson}>Continue learning <ArrowRight data-icon="inline-end" /></Button></div><div className="absolute -right-10 -bottom-20 size-64 rounded-full border-[32px] border-primary-foreground/10" /><div className="absolute right-12 top-12 font-serif text-8xl text-primary-foreground/10">Aa</div></div><div className="rounded-[2rem] border border-border bg-card p-7"><div className="flex items-center justify-between"><span className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">This week</span><Flame className="size-5 text-accent" /></div><p className="mt-6 font-serif text-5xl">3<span className="ml-2 text-xl text-muted-foreground">days</span></p><p className="mt-2 text-sm text-muted-foreground">You’re building a habit.</p><div className="mt-8 flex items-end gap-2">{[35, 54, 78, 28, 90, 42, 18].map((height, i) => <div key={i} className="flex flex-1 flex-col items-center gap-2"><div className={`w-full rounded-t-md ${i === 2 ? 'bg-accent' : 'bg-muted'}`} style={{ height: `${height}px` }} /><span className="font-mono text-[9px] text-muted-foreground">{['M','T','W','T','F','S','S'][i]}</span></div>)}</div></div></section><section className="mt-12"><div className="flex items-end justify-between"><div><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Your curriculum</p><h2 className="mt-2 font-serif text-3xl">Keep exploring</h2></div><button onClick={() => setView('practice')} className="hidden items-center gap-1 text-sm font-medium text-primary sm:flex">See all <ChevronRight className="size-4" /></button></div><div className="mt-6 grid gap-4 md:grid-cols-3">{lessons.map((lesson, i) => <button key={lesson.title} onClick={i === 0 ? startLesson : undefined} className="group rounded-2xl border border-border bg-card p-5 text-left transition-all hover:-translate-y-1 hover:border-primary/40 hover:shadow-lg"><div className="flex items-start justify-between"><span className={`grid size-11 place-items-center rounded-xl ${lesson.color} text-primary-foreground`}><span className="font-serif text-xl">{i + 1}</span></span>{lesson.done ? <span className="grid size-6 place-items-center rounded-full bg-accent text-accent-foreground"><Check className="size-3" /></span> : <Lock className="size-4 text-muted-foreground" />}</div><p className="mt-8 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">{lesson.level}</p><h3 className="mt-1 font-serif text-2xl">{lesson.title}</h3><p className="mt-1 text-sm text-muted-foreground">{lesson.subtitle}</p></button>)}</div></section></div>
}

function PracticeView({ state, setState }: { state: PracticeState; setState: (state: PracticeState) => void }) {
  const videoRef = useRef<HTMLVideoElement>(null)
  const { start, stop, isReady, error } = useSignRecognition(videoRef)

  const active = state !== 'intro' && state !== 'permission'
  const message = state === 'searching' ? 'Finding your hand…' : state === 'almost' ? 'Almost there' : state === 'correct' ? 'That’s it!' : state === 'incorrect' ? 'Not quite' : 'Ready when you are'

  const handleEnableCamera = async () => {
    setState('searching')
    try {
      await start()
    } catch {
      setState('permission')
    }
  }

  const handleCloseCamera = () => {
    stop()
    setState('intro')
  }

  return <div className="mx-auto w-full max-w-5xl px-5 py-8 lg:px-10 lg:py-12"><div className="flex items-center gap-3 text-sm text-muted-foreground"><button onClick={() => setState('intro')} className="hover:text-foreground">Level 1</button><ChevronRight className="size-4" /><span className="text-foreground">Hello</span></div><div className="mt-8 grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><div className="flex flex-col justify-between rounded-[2rem] border border-border bg-card p-7 md:p-9"><div><span className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Sign 01 of 05</span><h1 className="mt-4 font-serif text-5xl">Hello</h1><p className="mt-4 leading-7 text-muted-foreground">Raise your dominant hand, palm facing out. Move it gently from side to side.</p><div className="mt-8 rounded-2xl bg-muted p-5"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Your goal</p><div className="mt-3 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-full bg-accent text-accent-foreground"><Check className="size-5" /></div><p className="text-sm font-medium">Match the sign shape</p></div></div>{error && <div className="mt-4 rounded-xl border border-destructive/30 bg-destructive/10 p-3 text-sm text-destructive">Camera error: {error.message}</div>}</div><div className="mt-10 flex items-center gap-3"><div className="h-1.5 flex-1 rounded-full bg-muted"><div className="h-full w-1/5 rounded-full bg-accent" /></div><span className="font-mono text-xs text-muted-foreground">1/5</span></div></div><div className="relative flex min-h-[420px] flex-col items-center justify-center overflow-hidden rounded-[2rem] bg-primary p-6 text-primary-foreground"><div className="absolute inset-0 opacity-20" style={{backgroundImage:'radial-gradient(circle at 1px 1px, currentColor 1px, transparent 0)',backgroundSize:'28px 28px'}} />{isReady && <video ref={videoRef} autoPlay playsInline muted className="absolute inset-0 h-full w-full object-cover opacity-70" />}<div className="relative z-10 text-center"><div className={`mx-auto grid size-36 place-items-center rounded-full border-2 ${state === 'correct' ? 'border-accent bg-accent/20' : state === 'incorrect' ? 'border-destructive bg-destructive/20' : 'border-primary-foreground/30 bg-primary-foreground/10'}`}><span className="font-serif text-7xl">☝</span></div><p className="mt-7 font-serif text-3xl">{message}</p><p className="mt-2 max-w-xs text-sm text-primary-foreground/60">{state === 'correct' ? 'You matched the sign perfectly.' : state === 'incorrect' ? 'Try lifting your hand a little higher.' : 'Position your hand inside the circle.'}</p></div>{active && <div className="absolute bottom-6 left-6 right-6 flex gap-2"><Button variant="secondary" className="flex-1" onClick={() => setState(state === 'incorrect' ? 'searching' : state === 'correct' ? 'intro' : 'correct')}>{state === 'correct' ? 'Next sign' : state === 'incorrect' ? 'Try again' : 'Check my sign'} <ArrowRight data-icon="inline-end" /></Button><Button variant="ghost" size="icon" className="text-primary-foreground hover:bg-primary-foreground/10" onClick={() => setState('intro')} aria-label="Reset practice"><RotateCcw /></Button></div>}</div></div>{(state === 'intro' || state === 'permission') && <div className="fixed inset-0 z-20 grid place-items-center bg-foreground/30 p-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-[2rem] bg-card p-7 shadow-2xl"><div className="flex items-start justify-between"><div className="grid size-12 place-items-center rounded-2xl bg-accent text-accent-foreground"><Camera className="size-5" /></div><button onClick={handleCloseCamera} aria-label="Close"><X className="size-5 text-muted-foreground" /></button></div><h2 className="mt-7 font-serif text-3xl">Let’s practice together.</h2><p className="mt-3 leading-7 text-muted-foreground">SignOff uses your camera to recognize your hand shape. Your video stays on your device and is never recorded.</p>{state === 'permission' && <div className="mt-5 rounded-xl border border-destructive/30 bg-destructive/10 p-3 text-sm text-destructive">Camera access is blocked. You can still use the demo controls below.</div>}<Button className="mt-7 w-full" onClick={handleEnableCamera}>Enable camera <Camera data-icon="inline-start" /></Button><button onClick={() => setState('searching')} className="mt-4 w-full text-center text-sm text-muted-foreground underline-offset-4 hover:underline">Continue without camera</button></div></div>}</div>
}

function ProgressView() { return <div className="mx-auto w-full max-w-5xl px-5 py-8 lg:px-10 lg:py-12"><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Your progress</p><h1 className="mt-3 font-serif text-5xl">Small steps, real momentum.</h1><div className="mt-10 grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-primary p-6 text-primary-foreground"><Trophy className="size-5" /><p className="mt-8 font-serif text-4xl">24%</p><p className="mt-1 text-sm text-primary-foreground/70">Course complete</p></div><div className="rounded-2xl border border-border bg-card p-6"><Flame className="size-5 text-accent" /><p className="mt-8 font-serif text-4xl">3</p><p className="mt-1 text-sm text-muted-foreground">Day streak</p></div><div className="rounded-2xl border border-border bg-card p-6"><BookOpen className="size-5 text-accent-foreground" /><p className="mt-8 font-serif text-4xl">12</p><p className="mt-1 text-sm text-muted-foreground">Signs learned</p></div></div><div className="mt-8 rounded-2xl border border-border bg-card p-6"><h2 className="font-serif text-2xl">Recent activity</h2><div className="mt-6 flex flex-col gap-4">{['Completed “Hello” lesson','Practiced for 8 minutes','Started Level 1'].map((item, i) => <div key={item} className="flex items-center gap-4 border-b border-border pb-4 last:border-0 last:pb-0"><span className="grid size-9 place-items-center rounded-full bg-muted"><Check className="size-4 text-accent-foreground" /></span><div><p className="text-sm font-medium">{item}</p><p className="text-xs text-muted-foreground">{i + 1} day{i ? 's' : ''} ago</p></div></div>)}</div></div></div> }

function ProfileView() { return <div className="mx-auto w-full max-w-5xl px-5 py-8 lg:px-10 lg:py-12"><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Your profile</p><div className="mt-6 flex items-center gap-5"><div className="grid size-20 place-items-center rounded-3xl bg-accent font-serif text-3xl font-bold">A</div><div><h1 className="font-serif text-4xl">Alex Morgan</h1><p className="mt-1 text-muted-foreground">Learning since June 2024</p></div></div><div className="mt-10 rounded-2xl border border-border bg-card p-6"><h2 className="font-serif text-2xl">Settings</h2><div className="mt-6 flex flex-col gap-4"><button className="flex items-center justify-between border-b border-border pb-4 text-left text-sm">Camera & privacy <ChevronRight className="size-4 text-muted-foreground" /></button><button className="flex items-center justify-between border-b border-border pb-4 text-left text-sm">Notifications <ChevronRight className="size-4 text-muted-foreground" /></button><button className="flex items-center justify-between text-left text-sm">Learning preferences <ChevronRight className="size-4 text-muted-foreground" /></button></div></div></div> }

export default function SignOffApp() { const [view, setView] = useState<View>('home'); const [practiceState, setPracticeState] = useState<PracticeState>('intro'); const startLesson = () => { setView('practice'); setPracticeState('intro') }; return <div className="min-h-screen bg-background"><div className="flex min-h-screen"><Sidebar view={view} setView={setView} /><div className="flex min-w-0 flex-1 flex-col"><Header setView={setView} /><main className="flex-1 pb-20 lg:pb-0">{view === 'home' && <HomeView setView={setView} startLesson={startLesson} />}{view === 'practice' && <PracticeView state={practiceState} setState={setPracticeState} />}{view === 'progress' && <ProgressView />}{view === 'profile' && <ProfileView />}</main></div></div><nav className="fixed bottom-0 left-0 right-0 z-10 flex justify-around border-t border-border bg-card/95 p-2 backdrop-blur lg:hidden">{[{id:'home' as View,label:'Learn',icon:Home},{id:'practice' as View,label:'Practice',icon:Camera},{id:'progress' as View,label:'Progress',icon:BarChart3},{id:'profile' as View,label:'Profile',icon:CircleUserRound}].map(({id,label,icon:Icon}) => <button key={id} onClick={() => setView(id)} className={`flex min-w-16 flex-col items-center gap-1 rounded-lg px-3 py-2 text-[10px] font-medium ${view === id ? 'text-primary' : 'text-muted-foreground'}`}><Icon className="size-5" />{label}</button>)}</nav></div> }
